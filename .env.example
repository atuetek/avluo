# Database
DATABASE_URL=postgresql://avluo:devpass@localhost:5432/avluo
POSTGRES_PASSWORD=devpass

# JWT
JWT_SECRET=change-me-32-chars-minimum-secret-change-me
JWT_EXPIRES_IN=7d

# MinIO
MINIO_USER=avluo
MINIO_PASSWORD=devpass
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false

# Redis
REDIS_URL=redis://localhost:6379

# SMS (Netgsm for TR)
NETGSM_USER=dev
NETGSM_PASSWORD=dev
NETGSM_SENDER=AVLUO

# Push (Web-Push)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:dev@avluo.app

# App
APP_BASE_URL=http://demo.localhost:4200
PLATFORM_URL=http://localhost:3000
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001

# WebAuthn / Passkeys
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:4200

# Production Object Storage (Hetzner S3) — leave empty for local MinIO
# S3_ENDPOINT=fsn1.your-objectstorage.com
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_USE_SSL=true
# MEDIA_PUBLIC_BASE_URL=https://fsn1.your-objectstorage.com

